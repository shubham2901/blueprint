"""
Blueprint Backend — Journey API (GET /api/journeys, GET /api/journeys/{id})

Stub router — endpoints will be implemented in Phase 5.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/journeys", tags=["journeys"])

# Phase 5: GET /api/journeys — list all journeys
# Phase 5: GET /api/journeys/{id} — get journey detail
